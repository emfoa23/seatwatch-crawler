# HTTP / 비동기
httpx>=0.27
tenacity>=8.5

# 인터파크 — Playwright 헤드리스 (SVG/Canvas 좌석맵)
playwright>=1.45

# Redis (Valkey 호환)
redis>=5.0

# Postgres
psycopg[binary]>=3.2

# 환경변수
python-dotenv>=1.0

# 관측
sentry-sdk>=2.0
